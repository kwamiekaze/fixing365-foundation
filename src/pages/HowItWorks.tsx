import { Link } from "@tanstack/react-router"; import { ClipboardList,SearchCheck,Wrench,ArrowRight,Camera,Clock3,ShieldCheck } from "lucide-react"; import { PageIntro } from "@/components/PageIntro"; import { Button } from "@/components/ui/button";
const steps=[[ClipboardList,'Tell us what’s broken','Describe the symptoms in your own words, select the nearest category, and add photos or a video.'],[SearchCheck,'Get matched with a qualified provider','The request points toward providers suited to the trade, property and qualification requirements.'],[Wrench,'Get it fixed','Choose between quotes or an appointment and share the timing that works for you.']];
export function HowItWorksPage(){return <main><PageIntro eyebrow="How it works" title="A calmer path from problem to repair." action={<Button asChild size="lg" variant="hero"><Link to="/request">Tell us what’s broken <ArrowRight/></Link></Button>}>You do not need to know the technical diagnosis. Start with what you can observe; the right trade can take it from there.</PageIntro><section className="section-space"><div className="page-shell grid gap-12 lg:grid-cols-3">{steps.map(([Icon,title,text],i)=>{const I=Icon as typeof ClipboardList;return <article key={title as string} className="border-t border-border pt-6"><div className="flex items-center justify-between"><I className="size-8 text-primary"/><span className="font-display text-5xl font-bold text-border">0{i+1}</span></div><h2 className="mt-12 font-display text-2xl font-bold">{title as string}</h2><p className="mt-4 leading-7 text-muted-foreground">{text as string}</p></article>})}</div></section><section className="section-space bg-panel"><div className="page-shell"><h2 className="font-display text-3xl font-bold">A stronger request helps everyone.</h2><div className="mt-10 grid gap-6 md:grid-cols-3">{[[Camera,'Show the condition','Wide and close-up photos help providers understand access and visible damage.'],[Clock3,'Set honest urgency','Choose emergency, 48 hours, this week or flexible so timing is clear.'],[ShieldCheck,'Know the qualification','Regulated work is directed toward appropriately qualified or licensed providers.']].map(([Icon,t,d])=>{const I=Icon as typeof Camera;return <div key={t as string} className="rounded-md border border-border bg-background p-6"><I className="text-primary"/><h3 className="mt-6 font-display text-lg font-bold">{t as string}</h3><p className="mt-3 text-sm leading-6 text-muted-foreground">{d as string}</p></div>})}</div></div></section></main>}
