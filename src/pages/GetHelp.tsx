import { Link } from "@tanstack/react-router"; import { AlertTriangle,ArrowRight,HelpCircle,ExternalLink } from "lucide-react"; import { PageIntro } from "@/components/PageIntro"; import { Button } from "@/components/ui/button"; import { services,excludedServicesNote } from "@/config/services";
export function GetHelpPage(){return <main><PageIntro eyebrow="Get help" title="You don’t need to know who to call yet." action={<Button asChild variant="hero" size="lg"><Link to="/request">Describe the problem <ArrowRight/></Link></Button>}>Start with the symptom, room, appliance or part of the property. Choose the nearest category below if one is obvious.</PageIntro><section className="section-space"><div className="page-shell grid gap-4 md:grid-cols-2">{services.map(s=><Link key={s.id} to="/request" search={{category:s.id}} className="group flex min-h-24 items-center justify-between rounded-md border border-border bg-card p-5 hover:border-primary"><div><h2 className="font-display font-bold">{s.name}</h2><p className="mt-1 text-sm text-muted-foreground">{s.services.slice(0,3).join(' · ')}</p></div><ArrowRight className="shrink-0 text-primary"/></Link>)}</div></section><section className="bg-panel py-14"><div className="page-shell grid gap-6 md:grid-cols-2"><div className="rounded-md border border-destructive/35 bg-destructive/10 p-6"><AlertTriangle className="text-destructive"/><h2 className="mt-5 font-display text-xl font-bold">Immediate danger?</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">For fire, gas odor, electrical arcing, major flooding, structural collapse or medical danger, leave the area when appropriate and contact local emergency services first.</p></div><div className="rounded-md border border-border bg-background p-6"><HelpCircle className="text-primary"/><h2 className="mt-5 font-display text-xl font-bold">Cleaning or property cleanup?</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{excludedServicesNote}</p><a href="https://kleanupcrew.com" target="_blank" rel="noreferrer" className="mt-5 inline-flex items-center gap-2 font-bold text-primary">Visit KleanupCrew.com <ExternalLink className="size-4"/></a></div></div></section></main>}
