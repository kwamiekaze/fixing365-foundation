import { PageIntro } from "@/components/PageIntro"; import { RequestForm } from "@/components/RequestForm";
export function RequestPage({category='',problem='' }:{category?:string;problem?:string}){return <main><PageIntro eyebrow="Request service" title="Tell us what’s broken.">Tell us what’s wrong, snap a photo, and we’ll match you with the right provider.</PageIntro><section className="section-space"><div className="page-shell"><RequestForm initialCategory={category} initialProblem={problem}/></div></section></main>}
