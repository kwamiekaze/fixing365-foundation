import { Link } from "@tanstack/react-router"; import { ArrowLeft,ArrowRight,ShieldCheck,X } from "lucide-react"; import { getService } from "@/config/services"; import { Button } from "./ui/button";
export function ServicePanel({selected,onClose}:{selected:string|null;onClose:()=>void}){const service=getService(selected);if(!service)return null;return <aside className="absolute inset-x-3 bottom-24 z-30 max-h-[53%] overflow-y-auto rounded-md border border-border bg-panel p-5 shadow-2xl backdrop-blur-xl md:inset-y-5 md:left-auto md:right-5 md:max-h-none md:w-[25rem] md:p-7"><div className="flex items-start justify-between"><span className="font-display text-xs font-bold uppercase text-primary">Selected station</span><Button size="icon" variant="ghost" onClick={onClose} aria-label="Close service details"><X/></Button></div><h2 className="mt-3 font-display text-2xl font-bold">{service.name}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{service.description}</p><ul className="mt-5 grid gap-3">{service.services.map(item=><li key={item} className="flex gap-3 text-sm"><span className="mt-2 size-1.5 shrink-0 rounded-full bg-primary"/>{item}</li>)}</ul>{service.licensedProvider&&<div className="mt-5 flex gap-3 rounded-md border border-cool/30 bg-cool/10 p-3 text-xs leading-5 text-cool"><ShieldCheck className="mt-0.5 size-4 shrink-0"/>Regulated work is matched with appropriately qualified or licensed providers.</div>}<Button asChild variant="hero" size="lg" className="mt-6 w-full"><Link to="/request" search={{category:service.id}}>Request this service <ArrowRight/></Link></Button><Button variant="ghost" className="mt-2 w-full" onClick={onClose}><ArrowLeft/>Back to full view</Button></aside>}
