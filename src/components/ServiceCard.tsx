import { Link } from "@tanstack/react-router"; import { ArrowUpRight, Wrench, Zap, Droplets, Wind, Refrigerator, PaintRoller, House, RadioTower } from "lucide-react"; import type { ServiceCategory } from "@/types";
const icons={Hammer:Wrench,Zap,Droplets,Wind,Refrigerator,PaintRoller,House,RadioTower};
export function ServiceCard({service,compact=false}:{service:ServiceCategory;compact?:boolean}){const Icon=icons[service.icon as keyof typeof icons]??Wrench;return <article className="group border-t border-border py-6 transition-colors hover:border-primary"><div className="flex items-start justify-between gap-4"><span className="grid size-11 shrink-0 place-items-center rounded-md bg-secondary text-primary"><Icon/></span><Link to="/request" search={{category:service.id}} className="grid size-11 shrink-0 place-items-center rounded-md border border-border text-muted-foreground hover:border-primary hover:text-primary" aria-label={`Request ${service.name}`}><ArrowUpRight/></Link></div><h3 className="mt-5 font-display text-xl font-bold">{service.name}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{service.description}</p>{!compact&&<ul className="mt-4 grid gap-2 text-sm text-foreground/85">{service.services.slice(0,4).map(item=><li key={item} className="flex gap-2"><span className="mt-2 size-1.5 shrink-0 rounded-full bg-primary"/>{item}</li>)}</ul>}{service.licensedProvider&&<p className="mt-4 text-xs font-bold uppercase text-cool">Qualified / licensed providers where required</p>}</article>}
