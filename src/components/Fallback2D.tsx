import { services } from "@/config/services";
export function Fallback2D({selected,onSelect}:{selected:string|null;onSelect:(id:string)=>void}){return <div className="absolute inset-0 overflow-y-auto bg-panel-strong px-4 pb-32 pt-32"><div className="mx-auto max-w-5xl"><p className="font-display text-xs font-bold uppercase text-primary">Simple view / Repair house directory</p><h2 className="mt-3 max-w-2xl font-display text-3xl font-bold">Tap the part of your property that needs attention.</h2><div className="mt-8 grid grid-cols-2 gap-3 md:grid-cols-4">{services.map((service,i)=><button key={service.id} onClick={()=>onSelect(service.id)} className={`min-h-36 rounded-md border p-4 text-left transition ${selected===service.id?'border-primary bg-primary/10':'border-border bg-card hover:border-primary'}`}><span className="font-display text-3xl font-bold text-primary">0{i+1}</span><span className="mt-5 block font-display text-sm font-bold">{service.name}</span></button>)}</div></div></div>}
