import { services,excludedServicesNote } from "@/config/services"; import { Wrench } from "lucide-react"; import { Button } from "./ui/button"; import { Sheet,SheetContent,SheetDescription,SheetHeader,SheetTitle,SheetTrigger } from "./ui/sheet"; import { Link } from "@tanstack/react-router";
export function AllServicesDrawer(){return <Sheet><SheetTrigger asChild><Button variant="inverse"><Wrench/>All Services</Button></SheetTrigger><SheetContent className="w-full overflow-y-auto border-border bg-panel-strong sm:max-w-xl"><SheetHeader><SheetTitle className="font-display text-2xl">All Fixing365 services</SheetTitle><SheetDescription>Choose a category, then tell us what’s happening.</SheetDescription></SheetHeader><div className="mt-7 grid gap-7">{services.map(service=><section key={service.id}><div className="flex items-center justify-between gap-4"><h3 className="font-display font-bold">{service.name}</h3><Button asChild size="sm" variant="outline"><Link to="/request" search={{category:service.id}}>Request</Link></Button></div><p className="mt-2 text-xs leading-5 text-muted-foreground">{service.services.join(" · ")}</p></section>)}</div><div className="mt-8 border-t border-border pt-6 text-sm text-muted-foreground">{excludedServicesNote} <a className="font-bold text-primary" href="https://kleanupcrew.com" target="_blank" rel="noreferrer">Visit KleanupCrew.com</a></div></SheetContent></Sheet>}
