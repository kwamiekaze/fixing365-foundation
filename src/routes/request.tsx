import {createFileRoute} from '@tanstack/react-router'; import {z} from 'zod'; import {RequestPage} from '@/pages/Request'; const searchSchema=z.object({category:z.string().optional().catch(undefined)}); export const Route=createFileRoute('/request')({validateSearch:searchSchema,head:()=>({meta:[{title:'Request Repair Service — Fixing365'},{name:'description',content:'Describe the problem, add media, set urgency and prepare a home or property repair request.'},{property:'og:title',content:'Request Repair Service — Fixing365'},{property:'og:description',content:'Tell Fixing365 what is broken and prepare your repair request.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:RequestRoute}); function RequestRoute(){const {category}=Route.useSearch();return <RequestPage category={category ?? ''}/>}
