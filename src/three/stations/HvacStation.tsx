import { useFrame } from "@react-three/fiber"; import { useRef } from "react"; import * as THREE from "three"; import { StationBase,Box,Cylinder,type StationProps } from "./StationBase"; import { Airflow } from "../effects/Airflow";
function CondenserFan({reduced}:{reduced:boolean}){const ref=useRef<THREE.Group>(null);useFrame((_,raw)=>{if(ref.current&&!reduced)ref.current.rotation.z+=Math.min(raw,.05)*1.2});return <group ref={ref} name="obj-hvac-condenser-fan" position={[.72,.94,.35]} rotation-x={-Math.PI/2}>{[0,1,2,3].map(i=><Box key={i} name={`obj-hvac-condenser-fan-blade-${i}`} p={[0,.2,0]} s={[.08,.4,.035]} color="#303b44"/>)}</group>}
export function HvacStation(p:StationProps){const reduced=p.reduced ?? false;return <StationBase id="hvac" label="Heating & Air" accent="#71c8ff" {...p}><Box name="obj-hvac-furnace-air-handler" p={[-.65,1,-.15]} s={[.82,1.8,.68]} color="#697883" metal={.5}/><Box name="obj-hvac-thermostat" p={[.02,1.42,-.42]} s={[.38,.28,.08]} color="#e1e4de"/><Box name="obj-hvac-short-duct-section" p={[-.65,2.05,-.2]} s={[.72,.32,.62]} color="#87969e" metal={.65}/><Box name="obj-hvac-vent" p={[0,2.05,-.36]} s={[.56,.16,.08]} color="#93a5ae"/><Box name="obj-hvac-filter" p={[-.08,.65,.25]} s={[.48,.62,.06]} color="#d5cab6"/><group name="obj-hvac-gauge-set" position={[.16,.68,.18]}>{[-.12,.12].map((x,i)=><group key={i}><Cylinder name={`obj-hvac-gauge-${i}`} p={[x,.18,0]} r={.1} h={.05} color={i?"#d9534f":"#438bd1"}/><Cylinder name={`obj-hvac-gauge-hose-${i}`} p={[x,-.08,0]} r={.018} h={.35} color={i?"#d9534f":"#438bd1"}/></group>)}</group><Box name="obj-hvac-wall-mounted-mini-split" p={[.72,1.88,-.35]} s={[1.02,.34,.3]} color="#dfe3df"/><Cylinder name="obj-hvac-outdoor-condenser" p={[.72,.52,.35]} r={.48} h={.85} color="#61717b"/><CondenserFan reduced={reduced}/><Airflow reduced={reduced}/></StationBase>}
