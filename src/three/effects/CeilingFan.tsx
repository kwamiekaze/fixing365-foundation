import { useFrame } from "@react-three/fiber"; import { useRef } from "react"; import * as THREE from "three";
export function CeilingFan({reduced=false}:{reduced?:boolean}){const ref=useRef<THREE.Group>(null);useFrame((_,raw)=>{if(ref.current&&!reduced)ref.current.rotation.y+=Math.min(raw,.05)*.7});return <group name="obj-electrical-ceiling-fan" position={[0,2.65,0]}><mesh><cylinderGeometry args={[.08,.08,.35,12]}/><meshStandardMaterial color="#657080" metalness={.7}/></mesh><group ref={ref} position={[0,-.2,0]}>{[0,1,2,3].map(i=><mesh key={i} rotation-y={i*Math.PI/2} position={[0,0,.42]}><boxGeometry args={[.15,.035,.85]}/><meshStandardMaterial color="#a8875c" roughness={.55}/></mesh>)}</group></group>}
